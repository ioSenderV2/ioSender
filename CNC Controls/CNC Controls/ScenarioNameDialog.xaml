<Window x:Class="CNC.Controls.ScenarioNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        d:DesignHeight="140" d:DesignWidth="360"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize" MinWidth="300"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Title="ioSender" Icon="App.ico">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="btnOk" Content="OK" Click="BtnOk_Click" IsDefault="True" IsEnabled="False" MinWidth="75" MinHeight="24"/>
            <Button x:Name="btnCancel" Content="Cancel" Click="BtnCancel_Click" IsCancel="True" MinWidth="75" MinHeight="24" Margin="6,0,0,0"/>
        </StackPanel>
        <StackPanel>
            <TextBlock x:Name="txtPrompt" TextWrapping="Wrap" MaxWidth="400" FontSize="13" Margin="0,0,0,8"/>
            <TextBox x:Name="txtName" MinWidth="260" TextChanged="TxtName_TextChanged"/>
        </StackPanel>
    </DockPanel>
</Window>
