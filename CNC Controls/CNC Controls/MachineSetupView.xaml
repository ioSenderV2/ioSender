<UserControl x:Class="CNC.Controls.MachineSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d"
             d:DesignHeight="515" d:DesignWidth="900">
    <!-- The wizard's step tabs are nodes in this shell's tree now (phase 2 of
         docs/Architecture-Settings-Nav-Overhaul.md). The wizard itself stays whole and is the content
         of every page; the shell drives it through ISettingsPageProvider.ShowPage(). -->
    <local:SettingsNavShell x:Name="nav" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
</UserControl>
