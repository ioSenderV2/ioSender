<UserControl x:Class="CNC.Controls.OverrideControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="240">
    <UserControl.Resources>
        <local:EncoderModeToColorConverter x:Key="EncoderModeConverter" />
        <local:BoolToColorConverter x:Key="ToColorConverter" />
        <local:LogicalNotConverter x:Key="BooleanConverter" />
            <Style x:Key="ResetButtonStyle" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Background="Transparent">
                            <Image Source="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Visible override slider: the default WPF slider's hairline track + tiny thumb are almost
             invisible (worse at high DPI). This gives a clear groove and a chunky thumb that scale with DPI. -->
        <Style x:Key="OverrideSliderStyle" TargetType="Slider">
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TickBar Grid.Row="0" Placement="Top" Height="4" Margin="0,0,0,1" Fill="#FF8A9099" Ticks="{TemplateBinding Ticks}"/>
                            <Border Grid.Row="1" Height="6" CornerRadius="3" Background="#FFDADDE1" BorderBrush="#FF8A9099" BorderThickness="1" SnapsToDevicePixels="True"/>
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Focusable="False" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Focusable="False" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="13" Height="20" Cursor="Hand">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border CornerRadius="2" Background="#FF5A6066" BorderBrush="#FF2E3236" BorderThickness="1" SnapsToDevicePixels="True"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
        <Label x:Uid="lbl_override" x:Name="lblOverride" HorizontalContentAlignment="Right" Content="Override" VerticalAlignment="Center" VerticalContentAlignment="Center" MinWidth="75"/>
        <Slider x:Uid="sldOverride" x:Name="sldOverride" Width="100" Style="{StaticResource OverrideSliderStyle}" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="10" LostMouseCapture="Slider_LostMouseCapture" GotMouseCapture="Slider_GotMouseCapture"
                ToolTip="Drag to change the override percentage."
                Value="{Binding SliderValue, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                Ticks="{Binding Ticks, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                TickFrequency="{Binding TickFrequency, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
        <TextBox x:Uid="txtOverride" x:Name="txtOverride" TextWrapping="NoWrap" Text="200%" IsReadOnly="True" MinHeight="24" MinWidth="40" Margin="4,0,0,0" VerticalAlignment="Center" HorizontalContentAlignment="Right" VerticalContentAlignment="Center">
            <TextBox.Background>
                <MultiBinding Converter="{StaticResource EncoderModeConverter}">
                    <Binding Path="OverrideEncoderMode" Mode="OneWay"/>
                    <Binding Path="EncoderMode" Mode="OneWay" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                </MultiBinding>
            </TextBox.Background>
            <TextBox.Foreground>
                <MultiBinding Converter="{StaticResource ToColorConverter}">
                    <Binding Path="OverrideDisabled" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                    <Binding>
                        <Binding.Source>
                            <Brush>Red</Brush>
                        </Binding.Source>
                    </Binding>
                    <Binding>
                        <Binding.Source>
                            <Brush>Black</Brush>
                        </Binding.Source>
                    </Binding>
                </MultiBinding>
            </TextBox.Foreground>
        </TextBox>
        <Button x:Uid="btnOvReset" x:Name="btnOvReset" Content="Resources\reset.png" Focusable="False" Style="{StaticResource ResetButtonStyle}" Margin="2,0,0,0" Height="22" Width="22" Click="btnOverrideClick"
                ToolTip="Reset the override to 100%."/>
    </StackPanel>
</UserControl>
