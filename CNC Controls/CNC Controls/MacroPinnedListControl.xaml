<UserControl x:Class="CNC.Controls.MacroPinnedListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="230">
    <!-- Compact main-page/left-column form of the Macros panel (the flyout form is MacroExecuteControl).
         Fixed ~4-row-tall ListBox, pinned macros floated to the top (in pinned order) then the rest -
         scroll for more, pin any of them from the same list. Each row's pin icon sends that macro to
         slot 0, pushing the rest down and auto-dropping whatever falls off slot 3; the icon is green
         while pinned, grey otherwise. No separate unpin control - re-pinning is how you keep a macro's
         spot fresh. -->
    <UserControl.Resources>
        <local:BoolToVisibleConverter x:Key="BoolToVisible"/>
        <local:NotBoolToVisibleConverter x:Key="NotBoolToVisible"/>
        <local:MacroPinnedColorConverter x:Key="PinnedColor"/>
        <system:String x:Uid="str_noMacrosPinned" x:Key="NoMacrosMessagePinned">No macros defined. Go to Settings: Macros to define.</system:String>
        <Style x:Key="PinGlyphButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="MacroRowButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="0,1"/>
        </Style>
        <!-- Plain row chrome - no selection highlight, since selection isn't a concept here. -->
        <Style x:Key="PlainListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <GroupBox x:Uid="grp_macrosPinned" Header="Macros" HorizontalAlignment="Left" VerticalAlignment="Top" MinWidth="220"
              Loaded="MacroPinnedListControl_Loaded">
        <StackPanel Margin="2,2,2,0">
            <TextBlock x:Uid="lbl_noMacrosDefinedPinned" Text="{StaticResource NoMacrosMessagePinned}" TextWrapping="Wrap" MaxWidth="200"
                       Visibility="{Binding IsEmpty, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisible}}"
                       HorizontalAlignment="Center" Margin="8,4" TextAlignment="Center" Foreground="#FF666666"/>
            <ListBox x:Name="macroListBox" BorderThickness="1" BorderBrush="#FFCCCCCC" Background="Transparent"
                     ItemContainerStyle="{StaticResource PlainListBoxItemStyle}"
                     ItemsSource="{Binding DisplayedItems, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     Height="132"
                     IsKeyboardFocusWithinChanged="MacroListBox_FocusWithinChanged"
                     Visibility="{Binding IsEmpty, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NotBoolToVisible}}"
                     ToolTip="Pinned macros first, then the rest - scroll for more, click the pin icon to pin/refresh a spot (top 4 shown collapsed elsewhere).">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="{Binding Name}" Style="{StaticResource MacroRowButtonStyle}"
                                    Click="macroButton_Click" Tag="{Binding}" ToolTip="Run this macro."/>
                            <Button Grid.Column="1" Style="{StaticResource PinGlyphButtonStyle}"
                                    Click="pinButton_Click" Tag="{Binding}" ToolTip="Pin to the top.">
                                <Border Width="11" Height="11" CornerRadius="6" BorderThickness="1" BorderBrush="#FF666666"
                                        Background="{Binding Converter={StaticResource PinnedColor}}"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </GroupBox>
</UserControl>
