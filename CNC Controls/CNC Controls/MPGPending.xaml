<Window x:Class="CNC.Controls.MPGPending"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CNC.Controls"
        mc:Ignorable="d"
        Title="Sender" Height="165" Width="460" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Closing="Window_Closing" Icon="App.ico">
    <Grid Background="Snow">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Uid="lbl_mpgModeActive" x:Name="label" Grid.Row="0" Content="Pendant active, waiting for it to be disabled..." HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center" FontSize="16"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
            <Button x:Uid="btn_mpgContinue" x:Name="btnContinue" Content="Continue" Width="100" Height="26" Margin="6,0" Click="btnContinue_Click"/>
            <Button x:Uid="btn_mpgDisconnect" x:Name="btnDisconnect" Content="Disconnect" Width="100" Height="26" Margin="6,0" IsCancel="True" Click="btnDisconnect_Click"/>
        </StackPanel>
    </Grid>
</Window>
