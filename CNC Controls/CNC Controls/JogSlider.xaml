<UserControl x:Class="CNC.Controls.JogSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d"
             x:Name="root"
             d:DesignHeight="44" d:DesignWidth="140">
    <UserControl.Resources>
        <Style x:Key="OvalStepButton" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="bg" Stroke="#FFA6A6A6" StrokeThickness="1">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                        <GradientStop Color="#FFF2F2F2" Offset="0.5"/>
                                        <GradientStop Color="#FFD6D6D6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <Ellipse x:Name="press" Opacity="0">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFC4D8EF" Offset="0"/>
                                        <GradientStop Color="#FFA9C7E8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Stroke" Value="#FF5B9BD5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="press" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Left" Style="{StaticResource OvalStepButton}" Click="Dec_Click" ToolTip="Lower">
            <Rectangle Width="14" Height="3" RadiusX="1.5" RadiusY="1.5" Fill="#FF222222"/>
        </Button>
        <Button DockPanel.Dock="Right" Style="{StaticResource OvalStepButton}" Click="Inc_Click" ToolTip="Higher">
            <Grid Width="14" Height="14">
                <Rectangle Width="14" Height="3" RadiusX="1.5" RadiusY="1.5" Fill="#FF222222" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <Rectangle Width="3" Height="14" RadiusX="1.5" RadiusY="1.5" Fill="#FF222222" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Button>
        <TextBlock Text="{Binding ValueText, ElementName=root}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"/>
    </DockPanel>
</UserControl>
