{
  "_comment": [
    "This project's inputs to the shared turn-loop hooks in claude-hub/hooks/.",
    "The mechanism lives in the hub; every string specific to ioSender lives here.",
    "Layered over hub defaults.json - `denies` merge by name, so 'push' below only retunes the",
    "hub's rule where it differs. Canonical doc: docs/playbooks/turn_workflow_loop.md."
  ],

  "project": "ioSender",

  "source": {
    "extensions": ["cs", "xaml", "csproj", "resx", "config"]
  },

  "verify": {
    "pattern": "build\\.ps1(?=.*-Launch)",
    "command": ".\\build.ps1 -Launch -message=\"what we're testing\"",
    "label": "final build + launch"
  },

  "denies": [
    {
      "name": "testserver",
      "enabled": true,
      "match": "-testserver",
      "requiresAlso": "-Launch|ioSender|Start-Process",
      "altMatch": "IOSENDER_TESTSERVER\\s*=",
      "allowWhenPromptMatches": "testserver|test server|ui automation",
      "mustInclude": {
        "match": "-simulator",
        "message": "BLOCKED: -testserver without -simulator.\n\nEven when requested, the test server may only ever drive the simulator - never the real controller. Add -simulator, and confirm the target with GET /state/lbl_connectionTarget before any MDI, motion, homing or reset call."
      },
      "message": "BLOCKED: -testserver was not requested in this turn's prompt.\n\nThe rule (docs/playbooks/turn_workflow_loop.md, Hard rules): never reach for -testserver on your own initiative, only when the user asks for it in THIS turn, and even then only against -simulator. On 2026-07-21 an unrequested testserver run sent MDI commands to the real controller mid-homing.\n\nBuild and hand off instead - the user tests. If you believe you need it, ask."
    },
    {
      "name": "push",
      "enabled": true,
      "match": "\\bgit\\s+push\\b|push-all\\.ps1|\\bgh\\s+release\\s+create\\b",
      "allowWhenPromptMatches": "\\bpush\\b|\\brelease\\b|\\bship\\b|\\bpublish\\b|wrap.?up|end.?of.?session|create.{0,20}\\brepo|\\bremote\\b|\\bgithub\\b",
      "message": "BLOCKED: push/release is not part of a turn.\n\nThe rule (docs/playbooks/turn_workflow_loop.md, Hard rules): pushing to v2/master fires the release CI. That belongs to end_of_session_wrapup.md at actual session end, not to finishing a work item.\n\nCommit locally and hand off. The user will say when it is time to push."
    }
  ],

  "checklist": [
    "== TURN LOOP (docs/playbooks/turn_workflow_loop.md - canonical, follow all of it) ==",
    "1. PLAN FIRST. Before the first Edit/Write, state in your visible response what you understood and",
    "   how you intend to do it. A plain question (\"can we...\", \"what if...\") is a DISCUSSION - answer it,",
    "   do not implement. Record it: c:\\github\\claude-hub\\tools\\turn.ps1 plan \"<restated ask + approach>\"",
    "2. IMPLEMENT. One AskUserQuestion call at a time. New UI control -> inline x:Uid as you create it.",
    "   On a rename, grep the old identifier repo-wide BEFORE the first build.",
    "3. INTERIM BUILDS: .\\build.ps1 -Scratch, and only when you must compile-check while a live instance",
    "   is mid-test. Not reflexively before every -Launch.",
    "4. COMMIT AS YOU GO - each piece the moment it is verified. Never save commits up.",
    "5. FINAL BUILD, after the last commit, is the only one with -Launch:",
    "   .\\build.ps1 -Launch -message=\"what we're testing\"",
    "6. LOCALE, while the user tests: $env:PYTHONIOENCODING=\"utf-8\"; python tools/locadd.py",
    "7. HAND OFF - say plainly what to test. The turn ends here.",
    "",
    "HARD RULES (hook-enforced, do not attempt): no -testserver unless asked THIS turn and only against",
    "-simulator; no git push / release inside a turn (that is end-of-session); push remote is v2, not origin."
  ]
}
